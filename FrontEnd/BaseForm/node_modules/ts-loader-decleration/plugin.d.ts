export declare class DeclerationsPlugin {
    out: string;
    fs: any;
    main: string;
    constructor(options?: any);
    apply(compiler: any): void;
    replace(start: any, end: any, what: any): string;
}
